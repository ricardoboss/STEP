print("State your name: ")
string name = readline()

print("name is a ", typename(name))

if ((name ?? "") != "") {
    print(" and is not empty", EOL)
} else {
    print(" and is empty", EOL)
}

if (name == "John") {
    print("Hello John", EOL)
} else {
    if (name != "") {
        print("Welcome back, ", name, EOL)
    } else {
        print("Welcome", EOL)
    }
}
